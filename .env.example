# Discord
DISCORD_BOT_TOKEN=YOUR_DISCORD_BOT_TOKEN
CLIENT_ID=YOUR_APPLICATION_ID
# Optional: used by deploy-commands for guild-specific registration
GUILD_ID=YOUR_GUILD_ID

# MongoDB (required when using docker-compose with auth enabled)
MONGO_INITDB_ROOT_USERNAME=YOUR_MONGO_ROOT_USERNAME
MONGO_INITDB_ROOT_PASSWORD=YOUR_MONGO_ROOT_PASSWORD
MONGODB_URI=mongodb://YOUR_MONGO_ROOT_USERNAME:YOUR_MONGO_ROOT_PASSWORD@localhost:27017/xquare-community-manager?authSource=admin

# Ticket UI settings are stored in MongoDB via /ticket set
